# Agent notes — Rose / Newsey

When designing or calling **Jev** (TypeSafe System One) — article structuring, taxonomy questions, confidence gating, or `jev_analyses` — **use the TypeSafe skill**.

- Skill id: `typesafe-ai`
- Paths: `.agents/skills/typesafe-ai/` (official `npx skills` install) and `.cursor/skills/typesafe-ai/` (Cursor-native mirror)
- Invoke with `/typesafe-ai` or “use the TypeSafe skill”
- Live API key: `TYPESAFE_API_KEY` (see `.env.example`). If unset, use `fixtures/jev-mock/` — do not invent request/response fields.
- Product taxonomy: `docs/` in this repo (and the Rose project store)

Upstream: [typesafe-ai/skills](https://github.com/typesafe-ai/skills)
