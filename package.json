{
  "name": "newsey",
  "private": true,
  "type": "module",
  "description": "rose-bot — paced, robots-respecting news ingest (Postgres only)",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.js",
    "scrape-once": "node server.js --once"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
