{
  "name": "newsey",
  "private": true,
  "type": "module",
  "description": "rose-bot — paced, robots-respecting news ingest (HTTP client of rose-backend)",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.js",
    "scrape-once": "node server.js --once",
    "jev-once": "node jev-globe.js",
    "seed-globe-sample": "node scripts/seed-globe-sample.js",
    "test": "node --test jev-globe.test.js"
  }
}
